import { CreatePasswordResponse } from './CreatePasswordResponse';

// Same as CreatePasswordResponse but named differently for readability
export type UpdatePasswordResponse = CreatePasswordResponse;
